       <h3>Bienvenue sur votre compte</h3>
<div class="row accountPage">
  <div class="col-md-2">
    <div class="imageUser">
      <div align="center"> <img alt="User Pic" src="http://babyinfoforyou.com/wp-content/uploads/2014/10/avatar-300x300.png" class="img-circle img-responsive"> </div>
    </div>
  </div>
  <div class="col-md-10 ">

        <form action="/account/update" method="post">
          <div class="form-group">
            <label  for="prenom">Prénon:</label>
            <input type="text" class="form-control inputUpdate" id="name" name="name" value="{{user.name}}" placeholder="{{user.name}}" disabled="disabled">
          </div>
          <div class="form-group">
            <label  for="nom">Nom:</label>
            <input type="text" class="form-control inputUpdate" id="lastname" name="lastname" placeholder="{{user.lastname}}" value="{{user.lastname}}" disabled="disabled">
          </div>
          <div class="form-group">
            <label  for="username">Pseudo:</label>
            <input type="text" class="form-control inputUpdate" id="username" name="username" placeholder="{{user.username}}" value="{{user.username}}" disabled="disabled">
          </div>
          <div class="form-group">
            <label  for="bio">Bio:</label>
            <textarea type="text" class="form-control inputUpdate" id="bio" name="bio" disabled="disabled">{{user.bio}}</textarea>
          </div>
          <div class="form-group">
            <label  for="email">Email:</label>
            <input type="text" class="form-control inputUpdate" id="email" name="email"  placeholder="{{user.email}}" value="{{user.email}}" disabled="disabled">
          </div>
          <a type="button" href="#" id="edit" class="btn btn-default">Edit</a>
          <button type="submit" class="btn btn-default inputUpdate" disabled="disabled">Submit</button>
        </form>




  </div>
</div>
